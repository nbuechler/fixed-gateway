source /home/ec2-user/.bash_profile

run()
{
    nohup node /webapps/fixed-gateway/cluster_app.js >logfile.txt 2>&1 </dev/null &
    return 0
}

pwd
echo this is before the run function
run
echo end start_server script
